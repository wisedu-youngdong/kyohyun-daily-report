node_modules
dist
.env
.env.local
.DS_Store

# Firebase 에뮬레이터 / 마이그레이션 스크립트 관련 — 절대 커밋하면 안 됨
firestore-debug.log
ui-debug.log
firebase-debug.log
.firebase/
serviceAccountKey*.json
*-service-account*.json
.vercel
.env*

# 교무실 로비 목업 등 개인 프로토타입 에셋 — 이 앱과 무관하고 용량만 큼(4MB+).
# 로컬에는 그대로 두고 저장소에서만 제외한다.
office-assets/
