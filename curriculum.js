// ============================================================
// 표준 단원표 — 자동완성 제안 + 단원 정규화 키(unitKey) 매칭용
// ============================================================
// [검수 필요] 표준 교육과정 기준 초안입니다. 실제 수업 교재(디딤돌 등)의
// 단원 구성과 다른 부분은 교현학원 기준으로 수정해주세요.
//
// 구조: CURRICULUM[과목][코스] = [단원명, ...]
// - 코스 키는 '초5-1'(학년-학기), 고등은 과목명, 영어는 영역명
// - 단원명 배열 순서 = 실제 단원 순서 (자동완성 정렬에 사용)
// - 과목 확장 시(국어/사회/과학) 이 파일에 키만 추가하면 됨

export const CURRICULUM = {
  수학: {
    // ── 초등 (학년-학기) ──
    '초1-1': ['9까지의 수', '여러 가지 모양', '덧셈과 뺄셈', '비교하기', '50까지의 수'],
    '초1-2': ['100까지의 수', '덧셈과 뺄셈(1)', '여러 가지 모양', '덧셈과 뺄셈(2)', '시계 보기와 규칙 찾기', '덧셈과 뺄셈(3)'],
    '초2-1': ['세 자리 수', '여러 가지 도형', '덧셈과 뺄셈', '길이 재기', '분류하기', '곱셈'],
    '초2-2': ['네 자리 수', '곱셈구구', '길이 재기', '시각과 시간', '표와 그래프', '규칙 찾기'],
    '초3-1': ['덧셈과 뺄셈', '평면도형', '나눗셈', '곱셈', '길이와 시간', '분수와 소수'],
    '초3-2': ['곱셈', '나눗셈', '원', '분수', '들이와 무게', '자료의 정리'],
    '초4-1': ['큰 수', '각도', '곱셈과 나눗셈', '평면도형의 이동', '막대그래프', '규칙 찾기'],
    '초4-2': ['분수의 덧셈과 뺄셈', '삼각형', '소수의 덧셈과 뺄셈', '사각형', '꺾은선그래프', '다각형'],
    '초5-1': ['자연수의 혼합 계산', '약수와 배수', '규칙과 대응', '약분과 통분', '분수의 덧셈과 뺄셈', '다각형의 둘레와 넓이'],
    '초5-2': ['수의 범위와 어림하기', '분수의 곱셈', '합동과 대칭', '소수의 곱셈', '직육면체', '평균과 가능성'],
    '초6-1': ['분수의 나눗셈', '각기둥과 각뿔', '소수의 나눗셈', '비와 비율', '여러 가지 그래프', '직육면체의 부피와 겉넓이'],
    '초6-2': ['분수의 나눗셈', '소수의 나눗셈', '공간과 입체', '비례식과 비례배분', '원의 넓이', '원기둥, 원뿔, 구'],

    // ── 중등 (학년-학기) ──
    '중1-1': ['소인수분해', '정수와 유리수', '문자의 사용과 식', '일차방정식', '좌표평면과 그래프'],
    '중1-2': ['기본 도형', '작도와 합동', '평면도형의 성질', '입체도형의 성질', '자료의 정리와 해석'],
    '중2-1': ['유리수와 순환소수', '식의 계산', '일차부등식', '연립일차방정식', '일차함수와 그래프', '일차함수와 일차방정식의 관계'],
    '중2-2': ['삼각형의 성질', '사각형의 성질', '도형의 닮음', '피타고라스 정리', '확률'],
    '중3-1': ['제곱근과 실수', '다항식의 곱셈과 인수분해', '이차방정식', '이차함수'],
    '중3-2': ['삼각비', '원의 성질', '대푯값과 산포도', '상관관계'],

    // ── 고등 (2022 개정 교과서 기준 — 2026년 학년별 적용) ──
    // 구 명칭 대응: 수학I≈대수, 수학II≈미적분I, 미적분≈미적분II, 수학(상)(하)≈공통수학1/2
    // 단원명이 사실상 동일해 매칭에는 영향 없음. 전환기 고3은 그때그때 코멘트로 처리.
    '고등-공통수학1': ['다항식', '방정식과 부등식', '경우의 수', '행렬'],
    '고등-공통수학2': ['도형의 방정식', '집합과 명제', '함수와 그래프'],
    '고등-대수': ['지수함수와 로그함수', '삼각함수', '수열'],
    '고등-미적분I': ['함수의 극한과 연속', '미분', '적분'],
    '고등-미적분II': ['수열의 극한', '미분법', '적분법'],
    '고등-확률과통계': ['경우의 수', '확률', '통계'],
    '고등-기하': ['이차곡선', '공간도형과 공간좌표', '벡터'],
  },

  영어: {
    // 영어는 교과서 단원명이 출판사마다 달라 정규화 가치가 낮음.
    // 문법/읽기/쓰기/듣기 4개 영역 구성 — 세부 토픽은 실사용 보며 추가/삭제 예정
    '문법': [
      '문장의 형식', '시제', '조동사', '수동태',
      'to부정사', '동명사', '분사와 분사구문',
      '관계대명사', '관계부사', '접속사', '가정법',
      '비교', '명사와 관사', '대명사', '형용사와 부사',
      '전치사', '일치와 화법', '특수구문(도치·강조)',
    ],
    '읽기': ['지문 독해', '주제·요지 파악', '빈칸 추론', '어휘'],
    '쓰기': ['영작', '서술형', '문장 전환'],
    '듣기': ['듣기 평가', '받아쓰기'],
  },

  // 추후 확장 예시 (지금은 비워둠):
  // 국어: { ... },
  // 사회: { ... },
  // 과학: { ... },
};

// ── 정규화 매칭 헬퍼 ──
// 자유 텍스트(강사 입력 or AI 사진 분석 결과)를 표준 단원에 매칭.
// 매칭 성공 시 '{과목}|{코스}|{단원명}' 형태의 unitKey 반환, 실패 시 null.
// 리포트 저장 시 unit(자유 텍스트)과 별도로 unitKey를 함께 저장해두면
// 종합/3개월 리포트 집계 때 입력 표기가 달라도 같은 단원으로 묶임.
//
// 매칭 규칙 (오매칭 방지):
// 1) 전 코스에서 정확 일치를 최우선 — "분수"는 초3-2 '분수'에 정확히 매칭
//    (preferredCourse가 주어지면 해당 코스의 정확 일치를 먼저 확인)
// 2) 부분 일치는 "입력이 단원명을 포함"하는 방향만 허용하고("소수의 나눗셈 연습" → '소수의 나눗셈'),
//    후보가 여럿이면 가장 긴 단원명 선택 — "소수의 나눗셈"이 '나눗셈'보다 우선
// 3) 3글자 미만 단원명('원' 등)은 부분 일치 대상에서 제외 — "원기둥"이 '원'에 흡수되는 것 방지
export function findUnitKey(subject, freeText, preferredCourse = null) {
  if (!freeText || !CURRICULUM[subject]) return null;
  // "3단원 소수의 나눗셈" → "소수의나눗셈" 처럼 번호/공백 제거 후 비교
  const clean = freeText.replace(/^\d+\s*단원\s*/, '').replace(/\s/g, '');
  if (!clean) return null;

  const courseEntries = Object.entries(CURRICULUM[subject]);
  // preferredCourse가 있으면 그 코스를 맨 앞으로 (동명 단원의 코스 결정에 사용)
  if (preferredCourse && CURRICULUM[subject][preferredCourse]) {
    courseEntries.sort(([a], [b]) => (a === preferredCourse ? -1 : b === preferredCourse ? 1 : 0));
  }

  // 1차: 정확 일치
  for (const [course, units] of courseEntries) {
    for (const unit of units) {
      if (clean === unit.replace(/\s/g, '')) {
        return `${subject}|${course}|${unit}`;
      }
    }
  }

  // 2차: 입력이 단원명을 포함하는 경우만, 가장 긴 단원명 우선
  let best = null;
  for (const [course, units] of courseEntries) {
    for (const unit of units) {
      const unitClean = unit.replace(/\s/g, '');
      if (unitClean.length < 3) continue;
      if (clean.includes(unitClean) && (!best || unitClean.length > best.len)) {
        best = { key: `${subject}|${course}|${unit}`, len: unitClean.length };
      }
    }
  }
  return best ? best.key : null;
}

// 단원 "번호"만 뽑아내는 헬퍼 — findUnitKey는 이름 기준 매칭이라 강사가 이름 없이
// 번호만 적은 경우("2~3단원", "4단원,5단원")는 전혀 못 잡아서, 그런 리포트가 통계에서
// 원문 그대로 따로따로 쪼개지는 문제가 있었음(단원별 이해도 카드가 "4단원,5단원" 식으로
// 원문째 하나의 그룹이 되어버림). 번호 단위로 잘게 쪼개 여러 단원 통계에 각각 반영할 수
// 있도록 숫자만 뽑아 배열로 돌려준다. 이름 매칭(findUnitKey)이 이미 성공하는 케이스는
// 건드리지 않고, 번호가 있는 텍스트만 이 경로를 탄다 — 호출부에서 분기해서 사용.
//
// "2~3단원"/"2-3단원" 같은 범위 표기는 그 사이 번호를 전부 펼침(최대 10개 — 오타로
// 너무 큰 범위가 들어와 폭주하는 것 방지). "4단원,5단원"처럼 쉼표/공백으로 나열된 건
// 전역 정규식이 각각 매치하므로 별도 분리 로직 없이 다 잡힘.
export function extractUnitNumbers(freeText) {
  if (!freeText) return [];
  const numbers = new Set();
  const withoutRanges = freeText.replace(/(\d+)\s*[~-]\s*(\d+)\s*단원/g, (_, a, b) => {
    const start = parseInt(a, 10);
    const end = parseInt(b, 10);
    if (!Number.isNaN(start) && !Number.isNaN(end) && end >= start && end - start <= 10) {
      for (let n = start; n <= end; n++) numbers.add(String(n));
    }
    return ''; // 범위로 이미 처리한 부분은 지워서 아래 단일 패턴이 중복으로 다시 못 잡게
  });
  let match;
  const singleRe = /(\d+)\s*단원/g;
  while ((match = singleRe.exec(withoutRanges))) numbers.add(match[1]);
  return Array.from(numbers).sort((a, b) => Number(a) - Number(b));
}

// 자동완성 제안: 특정 코스의 단원 목록 반환 (칩 UI용)
export function getUnits(subject, course) {
  return CURRICULUM[subject]?.[course] || [];
}

// 과목별 코스 키 목록 (코스 선택 드롭다운용)
export function getCourses(subject) {
  return Object.keys(CURRICULUM[subject] || {});
}
